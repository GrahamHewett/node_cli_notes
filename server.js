const firstName = require('./utils');

console.log(firstName);

/*debugger in node and then
node inspect server.js 
use of inspect allows us to go to chrome://inspect/ in chrome and 
click the inspect link to use DevTools
when finished you can perform debugging again typing debug> restart
Ctrl+c or .exit to quit the debugger.*/